# content_searcher는 표준 라이브러리(tkinter 포함)만으로도 실행됩니다.
# 아래 패키지는 선택 사항이며, 설치된 것만 해당 파일 형식 검색을 지원합니다.
# 하나도 설치하지 않아도 텍스트 계열 파일(txt, csv, json, py 등)과 hwpx는 검색됩니다.

PyMuPDF>=1.23      # .pdf 검색
openpyxl>=3.1      # .xlsx, .xlsm, .xltx, .xltm 검색
python-docx>=1.1   # .docx 검색
python-pptx>=0.6.23 # .pptx 검색
xlrd>=2.0          # .xls 검색
