# 테스트 실행에 필요한 패키지입니다: pip install -r requirements.txt -r requirements-dev.txt
# tkinter가 포함된 Python이 필요합니다(GUI 클래스를 임포트하기 때문).

-r requirements.txt
pytest>=8.0
xlwt>=1.3   # 테스트에서 .xls 샘플 파일을 생성하기 위해서만 사용
